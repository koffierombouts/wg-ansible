---
# defaults file for wg-ansible

# Default values used in role
interface_name: "wg0"

# Default values for client config
vpn_server_port: "51820"
vpn_server_address: "10.8.0.1/24"
allowed_ips: "10.8.0.0/24"
keepalive: "25"

# Default values for server config
wg_dns_servers:
  - "8.8.8.8"
save_config: "true"
